@model E_Learning.Models.ViTriKNLValidation
@using E_Learning.Models
<link href="~/Content/assets/css/PagedList.css" rel="stylesheet" />
<script src="~/Content/assets/Chonse/chosen.jquery.js"></script>
<link href="~/Content/assets/Chonse/chosen.css" rel="stylesheet" />

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="modal-dialog" style="max-width:800px" id="formE">
        <div class="modal-content">
            <div class="modal-header">
                <h5 id="ModalTitle">Phân quyền đánh giá thủ công</h5>
                <a href="#" class="close" data-dismiss="modal">&times;</a>
            </div>
            <div class="modal-body">

                @*@Html.ValidationSummary(true, "", new { @class = "text-danger" })*@
                @Html.HiddenFor(m => m.MaViTri)
                <fieldset id="SubmitForm">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <label class=" mb-1 font-weight-bold float-left">Vị trí khung năng lực</label>
                                @Html.DropDownList("IDKNL", null, new { id = "IDVT", @class = "form-control", @style = "width:100%" })
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="form-group">
                                <label class=" mb-1 font-weight-bold float-left">Phòng Ban/Bộ Phận</label>
                                @Html.DropDownList("IDPB", null, new { id = "IDPhongBan", @class = "form-control", @style = "width:100%" })
                            </div>
                        </div>


                        <div class="col-md-12">
                            <div class="form-group text-xl-left font-weight-bold"> ĐÁNH GIÁ TRỰC TIẾP</div>
                            <div class="form-group text-xl-left font-weight-bold">
                                @if (MyAuthentication.IDQuyen != 9 && MyAuthentication.IDQuyen != 10)
                                {
                                    <div class="font-weight-bold text-left">Các vị trí đánh giá  &nbsp;<a class="fa fa-trash icon_action" id="Remove_NV1" title="Xóa" href="#"></a></div>
                                }

                                @*@Html.CheckBox("checkALL", false, new { @id = "checkALL_1", @class = "checkALL", })*@
                                <div id="ListNV1" class=" form-group text-xl-left font-weight-bold overflow-auto" style="max-height:200px">
                                    @foreach (var item in ViewBag.SelecVTTT)
                                    {
                                        <div class="col-md-12 font-weight-bold float-left text-left">
                                            @*<a class="fa fa-trash icon_action" title="Xóa" href="@Url.Action("DeleteNVIDKNL", "FPosition", new { manv = item.Value,IDPB =@ViewBag.SelecIDPB})"
                       onclick="return confirm('Bạn muốn cập nhật nhân viên : @item.Text')">
                    </a>*@
                                            <input type="checkbox" aria-label="Checkbox for following text input" name="CheckRemove1" id="@item.Value" class="checkALL_1">
                                            @item.Text
                                        </div>
                                    }
                                </div>

                                @Html.DropDownList("Selec", null, "", new { id = "multiSelectDropDownQAs", @class = "form-control multiSelectDropDownQAs", multiple = "multiple", })
                                @*@Html.ListBox("Selected", ViewBag.Selected, new { style = "width:90%" })*@

                            </div>
                            <div class="row">
                                <div class="col-md-4">
                                    <div class="form-group text-xl-left font-weight-bold">
                                        <label class=" mb-1 font-weight-bold float-left">Phân xưởng</label>
                                        @Html.DropDownList("IDPX1", null, "--Phân xưởng--", new { id = "IDPX1", @class = "form-control marginTB5 selec ChosenSL", })
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-group text-xl-left font-weight-bold">
                                        <label class=" mb-1 font-weight-bold float-left">Nhóm</label>
                                        @Html.DropDownList("IDNhom1", null, "--Nhóm--", new { id = "IDNhom1", @class = "form-control marginTB5 selec ChosenSL", })
                                    </div>
                                </div>
                                <div class="col-md-4">
                                    <div class="form-group text-xl-left font-weight-bold">
                                        <label class=" mb-1 font-weight-bold float-left">Tổ</label>
                                        @Html.DropDownList("IDTo1", null, "--Tổ--", new { id = "IDTo1", @class = "form-control marginTB5 selec ChosenSL", })
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-12">
                            <div class="form-group text-xl-left font-weight-bold"> KIỂM DUYỆT KẾT QUẢ ĐÁNH GIÁ</div>
                            <div class="form-group text-xl-left font-weight-bold">
                                @if (MyAuthentication.IDQuyen != 9 && MyAuthentication.IDQuyen != 10)
                                {
                                    <div class="font-weight-bold text-left">Các vị trí đánh giá  &nbsp;<a class="fa fa-trash icon_action" id="Remove_NV2" title="Xóa" href="#"></a></div>
                                }

                                @*@Html.CheckBox("checkALL", false, new { @id = "checkALL_2", @class = "checkALL", })*@
                                <div id="ListNV2" class=" form-group text-xl-left font-weight-bold overflow-auto" style="max-height:200px">
                                    @foreach (var item in ViewBag.SelecVTTC)
                                    {
                                        <div class="col-md-12 font-weight-bold float-left text-left">
                                            @*<a class="fa fa-trash icon_action" title="Xóa" href="@Url.Action("DeleteNVIDKNL", "FPosition", new { manv = item.Value,IDPB =@ViewBag.SelecIDPB})"
                       onclick="return confirm('Bạn muốn cập nhật nhân viên : @item.Text')">
                    </a>*@
                                            <input type="checkbox" aria-label="Checkbox for following text input" name="CheckRemove2" id="@item.Value" class="checkALL_2">
                                            @item.Text
                                        </div>
                                    }
                                </div>

                                @Html.DropDownList("Selected", null, "", new { id = "multiSelectDropDownQAs", @class = "form-control multiSelectDropDownQAs", multiple = "multiple", })
                                @*@Html.ListBox("Selected", ViewBag.Selected, new { style = "width:90%" })*@

                            </div>
                            <div class="row">
                                <div class="col-md-3">
                                    <div class="form-group text-xl-left font-weight-bold">
                                        <label class=" mb-1 font-weight-bold float-left">Phân xưởng</label>
                                        @Html.DropDownList("IDPB2", null, "--Bộ phận--", new { id = "IDPB2", @class = "form-control marginTB5 selec ChosenSL", })
                                    </div>
                                </div>
                                <div class="col-md-3">
                                    <div class="form-group text-xl-left font-weight-bold">
                                        <label class=" mb-1 font-weight-bold float-left">Phân xưởng</label>
                                        @Html.DropDownList("IDPX2", null, "--Phân xưởng--", new { id = "IDPX2", @class = "form-control marginTB5 selec ChosenSL", })
                                    </div>
                                </div>
                                <div class="col-md-3">
                                    <div class="form-group text-xl-left font-weight-bold">
                                        <label class=" mb-1 font-weight-bold float-left">Nhóm</label>
                                        @Html.DropDownList("IDNhom2", null, "--Nhóm--", new { id = "IDNhom2", @class = "form-control marginTB5 selec ChosenSL", })
                                    </div>
                                </div>
                                <div class="col-md-3">
                                    <div class="form-group text-xl-left font-weight-bold">
                                        <label class=" mb-1 font-weight-bold float-left">Tổ</label>
                                        @Html.DropDownList("IDTo2", null, "--Tổ--", new { id = "IDTo2", @class = "form-control marginTB5 selec ChosenSL", })
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-12">
                            <div class="font-weight-bold text-left">Các vị trí được phân quyền tự động &nbsp; @ViewBag.CountVT  Vị trí</div>
                            <div class=" form-group text-xl-left font-weight-bold overflow-auto" style="max-height:200px">
                                @foreach (var item in ViewBag.SelecDefault)
                                {
                                    <div class="col-md-12 font-weight-bold float-left text-left">
                                        @item.Text
                                    </div>
                                }
                            </div>
                        </div>

                    </div>

                    <div class="btn-margin-top text-center">
                        @if (MyAuthentication.IDQuyen != 9 && MyAuthentication.IDQuyen != 10)
                        {
                            <input type="submit" value="Xác nhận" class="btn btn-primary pull-right" />
                        }
                    </div>
                </fieldset>

            </div>
        </div>
    </div>
}


<script type="text/javascript">

    $(document).ready(function () {
        $(".multiSelectDropDownQAs").chosen({
            width: "100%",
        });
        $(".ChosenSL").chosen({
            width: "100%"
        })
        $("#multiSelectDropDownQAs_chosen .chosen-search-input").val("Chọn vị trí");

        $('.checkALL').click(function () {
            var id = $(this).attr('id');
            $('.' + id).prop('checked', this.checked)
        });
        //$(".selec").on("change", function () {
        //    var idpb = $(this).attr('id');
        //    var idv = $(this).val();

        //    if (idpb == "IDNhom") {
        //        $("#IDPX").append("<option value ='" + null + "'>" + "--Chọn Nhóm--" + "</option>");
        //        console.log("HHH", idpb, idv);
        //    }
        //});


        async function RemNV(url, params, method) {
            return $.ajax({
                url: url,
                type: method || 'GET',
                dataType: 'json',
                data: params
            });
        }

        $("#Remove_NV1").click(async function () {
            let arr = [];
            $("input[name='CheckRemove1']:checked").each(function () {
                arr.push($(this).attr('id'));
            });
            const promises = arr.map(async id => {
                const result = await RemNV("/FPosition/DeleteVTTT", { ID: id });
                return result;
            })
            const numFruits = await Promise.all(promises)
            $.get("/FPosition/getVTTTJSON", { id: $("#IDVT").val(), IDPB: $("#IDPhongBan").val() }, function (data1) {
                $("#ListNV1").empty();

                $.each(data1, function (index, row) {
                    $("#ListNV1").append(`<div class="col-md-12 font-weight-bold float-left text-left">
                                        <input type="checkbox" aria-label="Checkbox for following text input" name="CheckRemove1" id=${row.ID}>
                                        ${row.TenViTri}
                                    </div>`);
                });
            });
        });

        $("#Remove_NV2").click(async function () {
            let arr = [];
            $("input[name='CheckRemove2']:checked").each(function () {
                arr.push($(this).attr('id'));
            });
            const promises = arr.map(async id => {
                const result = await RemNV("/FPosition/DeleteVTTC", { ID: id });
                return result;
            })
            const numFruits = await Promise.all(promises)
            $.get("/FPosition/getVTTCJSON", { id: $("#IDVT").val(), IDPB: $("#IDPhongBan").val() }, function (data1) {
                $("#ListNV2").empty();
                $.each(data1, function (index, row) {
                    $("#ListNV2").append(`<div class="col-md-12 font-weight-bold float-left text-left">
                                        <input type="checkbox" aria-label="Checkbox for following text input" name="CheckRemove2" id=${row.ID}>
                                        ${row.TenViTri}
                                    </div>`);
                });
            });
        });

        //$(document).on("click", ".checkNV", function () {
        //    /*var iddex = $(this).attr('id').split("_")[1];*/
        //    $.get("/FPosition/CheckNV", { lsnv: $("#NVDG").val() }, function (data) {
        //        if (data[0] != null) {
        //            $("#dsnv").empty();
        //            /*$("#dsnv").append("<option value ='" + null + "'>" + "--Chọn Phân Xưởng--" + "</option>")*/
        //            $.each(data, function (index, row) {
        //                $("#dsnv").append("<div>" + row.HoTen + "</div>")
        //            });

        //        }
        //        else {
        //            $("#dsnv").empty();
        //        }
        //    });
        //});
    });

</script>
<style>
    .chosen-container-single .chosen-single {
        color: #495057;
        background: #fff;
        background-color: #fff;
        background-clip: padding-box;
        border: 1px solid #ced4da;
        border-radius: .25rem;
        transition: border-color .15s ease-in-out,box-shadow .15s ease-in-out;
        font-size: 1rem;
        font-weight: 400;
        line-height: 1.8;
        text-transform: capitalize;
    }

        .chosen-container-single .chosen-single span {
            text-transform: capitalize !important;
            font-size: 16px !important;
        }
</style>



